import { type NodeViewProps } from '@tiptap/react';

/**
 * Props for the HtmlEditorVideoNodeView component.
 *
 * Extends TipTap's NodeViewProps which provides access to the
 * node attributes, selection state, and attribute update function.
 */
export default interface IHtmlEditorVideoNodeViewProps extends NodeViewProps {}
