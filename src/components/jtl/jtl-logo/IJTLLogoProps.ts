/**
 * Prop definitions for the JTL Logo component.
 */
export default interface IJTLLogoProps {
  /**
   * Size variant of the logo
   * @default 'default'
   */
  size?: 'small' | 'default' | 'large';
}
