import { CheckboxGroupContextValue } from '../types';

/**
 * Props for CheckboxGroupContextProvider
 */
export default interface ICheckboxGroupContextProps extends React.PropsWithChildren<{ value: CheckboxGroupContextValue }> {}
