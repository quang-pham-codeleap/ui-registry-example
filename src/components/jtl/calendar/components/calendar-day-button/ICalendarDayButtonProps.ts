import React from 'react';
import { DayButton } from 'react-day-picker';

/**
 * Props for the CalendarDayButton component.
 * Extends all react-day-picker DayButton props directly.
 */
export default interface ICalendarDayButtonProps extends React.ComponentProps<typeof DayButton> {}
