/**
 * Type defining the possible sort orders for table columns
 * - 'ascend': Sort in ascending order
 * - 'descend': Sort in descending order
 * - null: No sorting applied
 */
type SortOrder = 'ascend' | 'descend' | null;

export default SortOrder;
