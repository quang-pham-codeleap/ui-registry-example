import { TableSize } from '../types';

/**
 * Default constants for DataTable component
 */
export const DEFAULT_TABLE_SIZE: TableSize = 'md';
export const DEFAULT_PAGE_SIZE = 1;
export const SELECTION_COLUMN_ID = 'selection';
export const ACTION_COLUMN_ID = 'jtl-table-default-row-action';

/**
 * Selection column sizing constants
 */
export const SELECTION_COLUMN_SIZE = 32;

/**
 * Action column sizing constants
 */
export const ACTION_COLUMN_SIZE = 60;

/**
 * Default empty content message
 */
export const DEFAULT_EMPTY_MESSAGE = 'No data available';

/**
 * Default column min size
 */
export const COLUMN_MIN_SIZE = 82;

/**
 * Default row height
 */
export const DEFAULT_ROW_HEIGHT = 72;

/**
 * Default table height
 */
export const DEFAULT_TABLE_HEIGHT = 500;

/**
 * Table header height (used in DataTableContainer for autoHeight calculations)
 */
export const TABLE_HEADER_HEIGHT = 50;
