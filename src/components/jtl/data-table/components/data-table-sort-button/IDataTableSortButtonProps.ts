import { SortDirection } from '../../types';

/**
 * Props for the DataTableSortButton component
 */
export default interface IDataTableSortButtonProps {
  /**
   * The sort direction of the column
   */
  sortDirection: SortDirection;
}
